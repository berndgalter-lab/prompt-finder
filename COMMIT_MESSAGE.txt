feat: Normalize PF enqueue system + add debug panel

## Summary
- ✅ NO ghost files found (no spaces in filenames)
- ✅ Added PF_DEBUG_ENQUEUES panel (?pfdebug=1 for admins)
- ✅ Normalized enqueue order with filemtime() cache-busting
- ✅ Added proof-of-life CSS rule (lime outline, temp)
- 🔧 FIXED: Re-enabled workflow-variables.css (was commented out)
- 🔧 FIXED: Added missing variables.js to module list (CRITICAL!)

## Files Modified
- functions.php (lines 1102-1351): Debug panel + normalize enqueues
- assets/css/pf-workflows-main.css: Proof-of-life + design tokens
- assets/css/components/workflow-variables.css: Enhanced badges/validation
- assets/js/modules/variables.js: Fixed renderer markup
- assets/js/modules/steps.js: Consistent renderer structure

## Enqueue Order (Deterministic)
CSS: core → main → header → sidebar → sections → variables → steps
JS: storage → variables → navigation → copy → progress → steps → keyboard → main

## Test URL
/workflows/test-workflow-all-fields/?pfdebug=1

## Ghost Files Removed
None found. Clean codebase.

## Breaking Changes
None. Backward compatible with archives/taxonomies using old system.

## Phase
1 of 2 (Diagnostic). Phase 2 will remove proof-of-life outline.

